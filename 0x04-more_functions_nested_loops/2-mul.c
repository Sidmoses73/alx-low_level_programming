#include "main.h"
/**
 * mul - This will checks for a digit (0 through 9).
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int res;

	int res = a * b;

	return (res);
}
