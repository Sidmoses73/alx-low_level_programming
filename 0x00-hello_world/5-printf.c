#include <stdio.h>
/**
 * main - Programe that prints using the function printf
 * Return: 0
 * programe must compile without warning using gcc option
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
